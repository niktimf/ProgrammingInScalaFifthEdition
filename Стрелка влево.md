#Scala #Декларативноепрограммирование 

* Стрелка влево (<-) - определяет текущий элемент цикла for. Отделяет переменную счетчик от генератора. Можно интерпритировать как in.

* Например: for n <- 1 to 10 ...